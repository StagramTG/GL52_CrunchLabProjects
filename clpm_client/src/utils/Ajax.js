import axios from 'axios'

/**
 * Class Ajax
 * Utils to perform Ajax request and fetch/modify data
 * from distant server.
 */
export default class Ajax {
    constructor(base_url) {
        this.base_url = base_url
    }

    get(path) {

    }

    post(path) {

    }
}